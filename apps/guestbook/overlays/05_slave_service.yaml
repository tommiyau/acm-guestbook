    kind: Service
    apiVersion: v1
    metadata:
      name: redis-slave
    spec:
      type: ClusterIP
      ports:
        - port: 6379
      selector:
        app: guestbook
        role: slave
        tier: backend
